---
title: "Reticulate Notebook"
output: html_notebook
---

```{r}
library(reticulate)
use_condaenv("py37")

```


```{python}
import nighres
import numpy as np
import nibabel as nib
import ants

```


```{r}
library(ANTsR)

fi <- antsImageRead( getANTsRData( "mni" ) )
invisible( plot(  fi, axis=3 ) )

fi@filename

Sys.setenv(fi_filename = fi@filename)
```

```{python}

# Load an image with nibabel...

bd='/data00/leonardo/layers/regdata/sub-02/ses-01/anat/'
nii = nib.load(bd + 'part_inv2_bc.nii.gz').get_fdata()


```


```{r}

# ...and display it with ants in R

nii_ants = as.antsImage(py$nii)
invisible(plot( nii_ants, axis=3 ))

```


```{bash, engine.opts="-l"}
ls ${fi_filename}
```


